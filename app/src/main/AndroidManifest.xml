<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.health_up">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterDonor"></activity>
        <activity android:name=".DoctorDetails" />
        <activity
            android:name=".PatientAppointments"
            android:label="@string/title_activity_patient_appointments"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DoctorSetAppointments" />
        <activity android:name=".RegisterPatient" />
        <activity android:name=".RegisterDoctor" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PatientDetails" />
        <activity android:name=".DoctorAppointments" />
        <activity
            android:name=".SearchDoctor"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
    </application>

</manifest>